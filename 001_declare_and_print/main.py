# 1. Declare the variables
instructor_name = "Alex"
num_students = 30
course_name = "Python"

# 2. Print them using an f-string (formatted string literal)
# An f-string allows you to embed variable values directly inside a string.
print(f"The instructor is {instructor_name}, there are {num_students} students in the {course_name} class.")
